feat: dashboard overhaul — hide empty widgets, polish dark/violet theme, fix OpenRouter

- Hide widgets with no data (empty states no longer clutter the dashboard)
- Only show cards that have real content; interactive widgets always visible
- Polish dark/violet theme: better gradients, glow effects, glassmorphism
- Metric cards highlight when they have real data, dim when empty
- Section cards get hover transitions and gradient backgrounds
- Add "Add widget" shortcut and empty-state CTA in widget grid
- Fix OpenRouter probe: use google/gemini-2.0-flash-exp:free (old model was 404)
- Better activity feed empty state with actionable copy

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
