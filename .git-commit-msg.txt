fix: DOS Box Lab — robust js-dos script loading with polling + CDN fallback

- Replace 500ms setTimeout with proper polling (100ms interval, 8s timeout)
- Load both local and CDN CSS simultaneously
- Try local script first, fall back to CDN if window.Dos not set
- Show "Loading DOS engine..." spinner while script loads
- Better error message if both sources fail

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
