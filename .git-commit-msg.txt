security(api): close credit-minting and add auth to admin/state/director routes

- Restrict credit/wallet balance mutation to admins and internal services
  via new `canMutateBalances` helper (`src/lib/authz.ts`).
- Require `reason` and `idempotencyKey` for all credit/wallet adjustments.
- Enforce admin-only access on `POST /api/tracks`.
- Require signed-in user on `GET /api/ha/state`.
- Authenticate `POST /api/director/plan` and replace `x-forwarded-host` SSRF
  vector with `URL.origin` from the incoming request.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
