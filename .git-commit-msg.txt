feat: wire Code scanner to live API, expand env docs, add behavioral tests

- Expand `.env.example` to document all referenced environment variables,
  grouped by subsystem.
- Update VS Code workspace settings to default to PowerShell and enable shell
  integration; add REST Client config and recommended extensions.
- Add terminal-server build/typecheck step to CI workflow.
- Downgrade `/api/litt/scan` and `/api/litt/file` from admin-only to signed-in
  users so the Code page can use them.
- Add `buildFileTree` helper and wire `src/app/code/page.tsx` to the live
  `/api/litt/scan` and `/api/litt/file` endpoints while keeping demo data as a
  fallback.
- Replace the string-based smoke test with behavioral tests for the code scanner,
  terminal auth token verification, and terminal security helpers.
- Harden terminal command blocking with regex patterns for destructive/piped
  commands and redactSecrets coverage.
- Add Vitest path alias resolution for `@/lib/*` imports.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
