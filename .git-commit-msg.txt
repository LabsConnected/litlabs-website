refactor: remove Jarvis naming and dead dependency, reconcile agent guidance

- Remove dead `@usejarvis/brain` dependency and `src/lib/TaskRepository.ts`.
- Rename `terminal-server/jarvis-ai.ts` → `littree-brain.ts` and replace the
  `jarvis:command` socket event with `brain:command`. Update terminal UI to use
  `brain <command>` and "LiTT Brain" labels.
- Update `.clinerules` / `.cursorrules` / `.devin-config.json` / `devin.config.json`
  to point to `reference-repo` as the canonical local checkout and document that
  `src/lib/litt-context.ts` is the brain, while `src/lib/litt.ts` is the
  notification dispatcher.
- Update `SETUP.md` to reference the `brain` terminal command.
- Rename future-dated `20261215_*` migrations to `20260714_*` to preserve
  lexicographic ordering.
- Fix remaining lint warnings: remove unused React Compiler suppression
  directives and resolve the `Image` icon vs DOM `Image` ambiguity in the library
  files page.
- Import the workspace deep-dive and dev-debugging docs into `docs/`.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
