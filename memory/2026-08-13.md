# 2026-08-13 — PC Audit & Hardening Day

## Project path
- Canonical repo confirmed: `C:\Users\litbi\CascadeProjects\litlabs-website` (up to date with `LabsConnected/litlabs-website.git`, working tree clean)
- Stale duplicates archived to `C:\Users\litbi\Archive\stale-projects-20260813\` (`LiTTreeLabstudios`, old `litlabs-website`) — contains `.env.local.bak`, do NOT cloud-sync

## Shell
- git-bash/MSYS only; god-core prompt wrapper swallow output. Use cmd.exe or file redirection for real results.

## Dev toolchain (all present)
Node v22.22.3 · npm 10.9.8 · pnpm 9.15.9 · git 2.54.0 · Python 3.11.9 · uv 0.11.16 · Vercel CLI 54.4.1 · Docker 29.5.3

## Windows state snapshot (today)
- OS: Windows 10 | 8 cores | 16 GB RAM | load ~4.5
- C: 931 GB (53% used) · E: 1.9 TB (7% used)
- RAM: ~3.5 GB free currently (Devin + Brave heavy)
- No WSL crash dumps, no `wsl-crashes` dump dir

## Startup / services (verified accurate, corrected 2026-08-13 19:20)
- Startup folders: EMPTY (both user + all-users)
- Stale services removed: Jarvis, NativePushService, GhostGuard, LitLabsApi, OllamaServer
- Stale tasks removed: GOD-CORE-DAEMON, SSHD-AutoStart, SystemMonitor, SoftLandingCreativeManagementTask
- SMB share to `C:\Users` (Everyone:Full): REMOVED. Only built-in admin$ C$ E$ IPC$ remain (expected).
- portproxy: 9 stale WSL-IP entries removed; only `0.0.0.0:8096 → 172.23.33.99` kept
- HP services: HPPSAService + HPPSASmartPrinting RUNNING (keep if using HP printer; else stop+disable). hpatchmon/shpamsvc = Windows services, KEEP (not HP).
- LiTree/Guardian/Forge/Tailscale = NOT FOUND anywhere (confirmed removed)
- ETW Host Service Updater v16 = Intel/JetBrains telemetry collector (keep, intentional)
- Disk 3 = phantom USB MassStorageClass (0 bytes, No Media, Offline, Healthy) — benign, ignore
- Google Updater: 2 instances (149 + 152). TM_AMSP gone; TM_LowerPriority_Recurring services stopped & removed from task cache. 152 remains active.

## Antivirus
- Trend Micro: fully uninstalled via Trend Remove.exe (MSI uninstall failed 1603, native remover worked). Folder deleted.
- Windows Defender: sole active AV now. WinDefend running, real-time ON, signatures current (v1.457.150.0), AMRunningMode=Normal, state 397568.
- QuickScan: 34s, 0 threats. 2 stale detections (MpTamperSrvDisableAV) cleared post-uninstall.

## Deferred user actions
- Reboot: pending (clears RestartPending on TPM, finalizes Defender init after AV switch)
- Secure Boot: enable in UEFI/BIOS (TPM healthy+ready, UEFI correct)
- BitLocker: deferred by user — TPM ready, drives decrypted
- Windows Update failures: SteelSeries driver (0x8024200B) retry in Settings; OpenAI Codex install (0x80073D02) blocked; VS Client Detector — all non-critical
- FullScan: optional after reboot (`Start-MpScan -ScanType FullScan`)
- Trend TM_LowerPriority_Recurring task: neutralized (script deleted), still shows in task list but runs nothing. Delete via admin PowerShell: `schtasks /delete /tn "TM_LowerPriority_Recurring" /f`

## OS-2D.1 — Studio CLI Runtime Convergence (evening session)

### What was done
- Added runId to ActiveCommand and LastResult in agent-core types
- Threaded runId through RuntimeStore.commandStart/commandEnd and CommandRouter.execWithTracking
- CommandRouter.dispatch() now accepts runId in args, threads to check/test/build
- terminal-server/command-bridge.ts passes runId through to router
- Created src/app/studio/context/LiTTRuntimeContext.tsx — LiTTRuntimeProvider + useStudioRuntime()
- Mounted LiTTRuntimeProvider in CommandStudio.tsx (wraps VoiceSessionProvider)
- LiTTLiveActivity.tsx now consumes useStudioRuntime() and shows:
  - RunId (shared with PowerShell litt runtime)
  - Active command from canonical RuntimeStore
  - Last result with duration
  - Heartbeat seq + freshness indicator
- LiTTAmbientHUD.tsx already consumed useLiTTRuntime() (parallel work)
- PowerShell adapter now displays RunId line in litt runtime output
- Added 18 integration tests (runId threading, event emission, snapshot serialization, CLI=Studio contract)
- Total agent-core tests: 203 (was 185)
- 10x stress: 203/203 pass, 0 failures

### Live bidirectional proof
- POST /internal/command (Studio path) returned runId: run_1786673439648_136ssl
- GET /internal/runtime (PowerShell path) returned lastResult.runId: run_1786673439648_136ssl
- PowerShell litt runtime displayed the same RunId
- MATCH confirmed: both surfaces see the same run identity

### Commits by parallel session
- e086a4fc — OS-2D cross-surface RuntimeStore routing
- e73a1f46 — OS-2D propagate runId through RuntimeStore
- a942552f — OS-2D debug/ship router commands + CLI --remote
- 38c8e657 — P1.1 contract corrections

### Uncommitted (my OS-2D.1 integration layer)
- src/app/studio/context/LiTTRuntimeContext.tsx (new)
- src/app/studio/components/CommandStudio.tsx (mount provider)
- src/app/studio/components/LiTTLiveActivity.tsx (runtime status bar)
- packages/litt-agent-core/src/__tests__/phase2d.test.ts (18 new tests)
- packages/litt-cli/scripts/litt-adapter.ps1 (RunId display)

### Key lesson
The parallel session committed the runId infrastructure changes I wrote. Always check git log before assuming work is uncommitted — a parallel agent may have already committed it.

## AppData cleanup (2026-08-13 19:30)
- CapCut cache: DELETED (~4 GB)
- Google DriveFS cache: DELETED (~2.1 GB)
- Hermes cache: DELETED (~4.3 GB)
- pnpm-cache: DELETED (~1.3 GB)
- Temp folder: cleaned from 366 MB to 52 MB
- Remaining Google AppData: 1.78 GB (Cloud SDK + Android Studio — dev tools, kept)
- CapCut/hermes/pnpm will regenerate caches as needed
