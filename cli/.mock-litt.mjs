const http=require("http");const server=http.createServer((req,res)=>{let b="";req.on("data",c=>b+=c);req.on("end",()=>{const body=JSON.parse(b);console.error("REQ body:",JSON.stringify({agentMode:body.agentMode,agentSlug:body.agentSlug,stream:body.stream,requestedModel:body.requestedModel,requestedProvider:body.requestedProvider,auth:req.headers.authorization}));res.writeHead(200,{"Content-Type":"text/event-stream","Cache-Control":"no-cache","Connection":"keep-alive"});res.write("data:"+JSON.stringify({type:"text",text:"The repo is a Next.js LiTT multi-agent studio."})+"\n\n");res.end("data: [DONE]\n\n");});});server.listen(3921,"127.0.0.1",()=>console.log("mock on 3921"));
