# LitLab Speed Wins — Implementation Status

Done:

- [x] Verify project state (dev/build/next.config already optimized)
- [x] Add SSE streaming to `/api/agents/chat` (Director + image-intent paths)
- [x] Add SSE streaming to `/api/ai-chat` (Canvas builder)
- [x] Wire `LITTTerminalShell` to use streaming (`sendAgent` + `/agent` slash cmd)
- [x] Add LRU cache to `src/lib/r2.ts` for `getSignedAudioUrl`
- [x] Convert `director-graph.ts` to pure function (back-compat class kept)
- [x] Add `streamText` import to `/api/ai-chat`

Pending user action:

- [ ] (optional) `pnpm add -D @next/bundle-analyzer` then wrap `next.config.ts` and run `ANALYZE=true pnpm build`
- [ ] (optional) Run Lighthouse on `/` and `/studio` to record the new baseline
- [ ] (optional) `npx tsc --noEmit` from a warm shell — the first run is ~3 min on this monorepo
