## Summary

Studio layout hierarchy polish pass — no runtime or feature changes.

- **Preview is now the default workspace surface** (was Work/Plan). Chat lives in the LiTT panel on the left; Preview gets the main workspace area, giving the intended Chat ~40% / Preview ~60% split on desktop.
- **Top bar simplified** to: `LiTT | Project | AUTO | Deploy | ...`
  - Removed workspace status popover trigger (absorbed into overflow menu)
  - Removed duplicate branch chip (repo name already in project picker)
  - Removed WritePermissionPill (write state visible in operator bar)
  - Added AUTO execution mode dropdown (moved from composer radio group)
- **Work logs collapse by default** in LiTTLiveActivity (was expanded). Low-level ops still auto-collapse on run completion.
- **Composer no longer renders PLAN/ACT/AUTO radio group** — the top bar dropdown owns execution mode. Intent pills and tool modes remain.
- Updated routing tests to expect Preview as the default active tab.

### Already in place (verified, no changes needed)
- Composer visually dominant with intent pills (Build/Fix/Design/Explain/Import) and secondary tool modes (Auto/Image/Music/Video/Code/Website)
- Idle/disconnected/empty operator states already hidden (StudioOperatorBar returns null when no content)
- Preview auto-refreshes on file changes via `studio:files-changed` event
- Panel resizing persists to localStorage via `useResizableWidth`
- Mobile/tablet reduces to chat-first with LiTT mobile sheet + bottom nav
- Studio uses intentionally dark surfaces regardless of site theme

#### Test plan
- [x] `tsc --noEmit` passes
- [x] `CommandStudio.routing.test.tsx` — 42/42 pass
- [x] `LiTTLiveActivity.test.tsx` — 15/15 pass
- [ ] Visual check: fresh Studio with no conversation
- [ ] Visual check: existing conversation
- [ ] Visual check: LiTT actively working
- [ ] Visual check: Approval required
- [ ] Visual check: Preview ready
- [ ] Visual check: Tool failure
- [ ] Visual check: Terminal connected
- [ ] Visual check: Mobile keyboard open
- [ ] Visual check: Narrow desktop window
- [ ] Visual check: Long conversation with lots of work logs

Generated with [Devin](https://devin.ai)
