# LiTTree Lab Studios — Deep Scan & "What's Needed" Report

**Date:** 2026-07-25
**Auditor:** Cline (act mode)
**Working dir:** `c:\Users\litbi\CascadeProjects\litlab`
**HEAD:** `7587afd0d0e61afe01d0cc90599bb10ec0077093`
**Previous scans:** `deep-scan-2026-07-21.md` (4 days old), `unused-scan-2026-07-20.md` (5 days old), `deep-dive-2026-07-14.md` (11 days old)

> ⚠️ **The 2026-07-21 deep-scan contains incorrect "✅ Resolved" claims.** It asserted that 8 dead `src/lib/` files and 8 orphaned `public/` assets had been removed. They have **NOT** been removed. The scan was reporting aspirational cleanup that never happened. This report supersedes it.

---

## 0. Executive Verdict

**Health:** 🟡 **Drifted.** The code is shipping, but the 7/21 report was over-confident. The cleanup the 7/20 + 7/21 scans recommended was **never executed**. The "two local checkouts" governance risk from 7/14 has been resolved at the config level (the archived checkout is now labelled `archived` in `.devin-config.json`) but `.clinerules` still points at the archived path. There is an in-flight Studio mobile redesign (6 files, uncommitted) that is the most likely cause of recent changes.

| Metric                              | Value                                                |
| ----------------------------------- | ---------------------------------------------------- |
| Open critical issues                | **0**                                                |
| Open high issues                    | **3** (governance drift + false docs + uncommitted work) |
| Open medium issues                  | **~9** (the "removed" cleanup that wasn't done)      |
| Open low issues                     | **~5**                                               |
| **Total open**                      | **~17**                                              |
| Files in `src/lib/`                 | 62 (4 directories + 58 files)                        |
| Dead `src/lib/` files (verified)    | **6** truly dead; 2 falsely accused (see §1)         |
| Orphaned public assets (verified)   | **7** (file/globe/next/vercel/window.svg + logo~2.png + og-image.png) |
| Uncommitted changes                 | **6 files**, +220/−183 lines                         |
| `tsc_errors.txt` at root            | **STILL EXISTS**                                     |

---

## 1. URGENT — The 7/21 scan was wrong about cleanup

The 7/21 deep-scan reported:

> "✅ Resolved | 8 dead `src/lib/` files + dup `AgentOrchestrator` class | `dir` of each → 'File Not Found'; `src/lib/` is now 64 live files"

**Verified 2026-07-25 — this is false.** Every one of those files is still on disk:

| File the 7/21 scan said was removed            | Actually there? | Real importers found today                |
| ----------------------------------------------- | --------------- | ----------------------------------------- |
| `src/lib/AgentOrchestrator.ts`                  | EXISTS          | 0 imports (real dead)                     |
| `src/lib/api-keys.ts`                           | EXISTS          | **1** (`/api/keys/create/route.ts`)       |
| `src/lib/api.ts`                                | EXISTS          | 0 imports (real dead)                     |
| `src/lib/color-templates.ts`                    | EXISTS          | **4 imports from 3 files**                |
| `src/lib/layout-schema.ts`                      | EXISTS          | 0 imports (real dead)                     |
| `src/lib/projects.ts`                           | EXISTS          | 0 imports (real dead)                     |
| `src/lib/skybox.ts`                             | EXISTS          | 0 imports (real dead)                     |
| `src/lib/agent-profiles.ts`                     | EXISTS          | 0 imports (real dead)                     |
| `public/file.svg` `globe.svg` `next.svg` `vercel.svg` `window.svg` | ALL EXIST | 0 references (real orphans)   |
| `public/logo~2.png`                             | EXISTS          | 0 references (real orphan — Windows "duplicate file" marker) |
| `public/og-image.png`                           | EXISTS          | 0 references (real orphan)                |
| `terminal-server/fly.toml`                      | EXISTS          | 0 CI references (real orphan)             |

**What 7/21 got right:** `render.yaml` and `public/og.png` were actually deleted.

**What 7/21 got wrong:**
- It counted deleted items in the "✅ Resolved" column without re-running `dir`.
- It treated the 7/20 scan's *recommendations* as if they had been *executed*.

**Action:** Re-run the actual cleanup as 4 small PRs, exactly as 7/20 laid out (see §3 below).

---

## 2. HIGH-severity issues (current)

### H-1. `.cursorrules` and `.clinerules` contradict each other and the codebase

| Claim                                                                  | Source          | Reality (verified today)         |
| ---------------------------------------------------------------------- | --------------- | -------------------------------- |
| `src/lib/AgentOrchestrator.ts` was deleted                             | `.cursorrules`  | FALSE — file still present       |
| `src/lib/AgentOrchestrator.ts` is the orchestration matrix             | `.clinerules`   | TRUE today, but the canonical version lives in `src/lib/agents.ts`; this file is dead code |
| `litt.ts` is the main LiT brain / assistant logic                      | `AGENTS.md`     | FALSE — `litt.ts` is a notification dispatcher; the "brain" is `litt-context.ts` |
| `litt.ts` is a notification dispatcher                                 | `.clinerules`   | TRUE                             |
| WORKSPACE_ROOT = `C:\Users\litbi\CascadeProjects\litlabs\reference-repo` | `.clinerules`   | FALSE — that path is labelled `archived` in `.devin-config.json`. The actual primary is `C:\Users\litbi\CascadeProjects\litlab` |
| WORKSPACE_ROOT = `C:\Users\litbi\CascadeProjects\litlab`               | `.cursorrules`  | TRUE                             |
| `paths.primary` = `C:/Users/litbi/CascadeProjects/litlab`              | `.devin-config.json` | TRUE                          |

**Two rule files, one correct, one wrong, and one contradictory doc.** This is the largest single risk in the project right now because every AI agent (Cline, Cursor, Composer) reads these files first.

**Action:**
1. Delete `src/lib/AgentOrchestrator.ts` (real dead code, contradicts the active `agents.ts`).
2. Update `.clinerules`: change `WORKSPACE_ROOT` to the current `litlab` path; remove `AgentOrchestrator.ts` from the subsystem list; add `agents.ts` instead.
3. Either delete `AGENTS.md` (its own header says "must not be edited as ground truth") or fix line 32 to describe `litt-context.ts` as the brain.
4. Re-confirm `.cursorrules` line 8-15 is now accurate (it currently is, conditional on the `AgentOrchestrator.ts` deletion).

### H-2. 6 uncommitted files (220 insertions / 183 deletions) — Studio mobile redesign

`git status --short` shows:

```
 M src/app/studio/components/StudioOS.tsx
 M src/app/studio/components/StudioSidebar.tsx
 M src/app/studio/components/StudioTopBar.tsx
 M src/app/studio/tools/ImageTool.tsx
 M src/components/MobileBottomNav.tsx
 M src/lib/navigation.ts
```

The diff shows a coherent **Studio mobile redesign**:
- `navigation.ts` — bottom nav reduced from 5 → 4 items (Studio, Projects, Agents, Account; Games/Settings moved into a "More" sheet).
- `MobileBottomNav.tsx` — now a flat 4-item grid, no more `MobileMoreSheet` state.
- `StudioOS.tsx` — added a **chat-first mobile layout**: chat is the base layer; non-chat tools open as a **bottom sheet overlay** with backdrop and close button. The 3-col grid is now `hidden md:grid`.
- `StudioSidebar.tsx` — refactored (146 line reduction, presumably the mobile rail moved out).
- `ImageTool.tsx` — 94 line additions, details TBD.
- `StudioTopBar.tsx` — 4 line tweak.

**Why this matters:**
- The redesign is **not committed**. A `git pull` or branch switch would lose it.
- The `MobileMoreSheet` component's import was removed from `MobileBottomNav.tsx` — **verify the component is still used elsewhere or it's now dead code.**
- `navigation.ts` still exports `MOBILE_MORE_ITEMS`, but `MobileMoreSheet` is no longer imported by `MobileBottomNav.tsx`. Either `MobileMoreSheet` is still rendered from a parent (likely `RootLayout`) or it became orphan.
- `pnpm build` and `pnpm type-check` have **not** been run on these changes (per `.todo.md`, the last `tsc --noEmit` was on the previous commit).

**Action:**
1. Decide commit vs. revert. If the redesign is good, commit it as one PR with a clear message.
2. Audit `MobileMoreSheet` — is it still used? If not, remove it (and possibly `MOBILE_MORE_ITEMS`).
3. Run `npx tsc --noEmit` and `pnpm build` on the branch.
4. Re-verify the file listing in `docs/deep-scan-2026-07-21.md` matches reality, because the scan was a "what we wish were true" document, not a "what is true" one.

### H-3. Autonomic Loop e2e has never been run (still active milestone)

`tasks/active.json` says:

> "Milestone: 'Autonomic Loop Wiring' — status: in_progress"
> "Next: verify the loop endpoints (`/api/director/plan`, `/api/agents`, `/api/memory`, `/api/agent-tasks`) return 2xx, fix any 4xx/5xx, and make sure the banner reflects live state."

The 7/21 scan verified the 4 GET endpoints return 200 with graceful degradation. **The POST path has never been exercised end-to-end.**

- `src/daemon.ts` has no `pnpm` script — must be launched manually.
- `src/proxy.ts` has no `pnpm` script — purpose unknown, verify callers.
- The banner is the only health surface; if the daemon is dead, the loop looks healthy.

**Action:** See 7/21 §H-2 — add `"daemon:dev": "tsx src/daemon.ts"` and run the smoke test sequence in `docs/deep-scan-2026-07-21.md` §6 step 10.

---

## 3. MEDIUM-severity issues (current)

### M-1. The recommended cleanup from 7/20 was never executed

The 7/20 scan provided a clean 4-PR plan. **None of it was done.** Re-running the plan today:

| PR (per 7/20)                                          | Status today    |
| ------------------------------------------------------ | --------------- |
| **PR 1** — `pnpm remove @usejarvis/brain jsnes @monaco-editor/react monaco-editor @stripe/stripe-js playwright` | Not done |
| **PR 2** — Delete `public/{file,globe,next,vercel,window}.svg`, `logo~2.png`, `og-image.png` | Not done |
| **PR 3** — Delete `src/lib/{api-keys,api,agent-profiles,color-templates,layout-schema,projects,skybox}.ts` + `AgentOrchestrator.ts` | Not done (but re-verify: `api-keys` and `color-templates` are NOT actually dead — see below) |
| **PR 4** — Delete `render.yaml` and `terminal-server/fly.toml`; fix or delete root `Dockerfile` | Partial (`render.yaml` removed; `fly.toml` still here; root `Dockerfile` still broken) |

**Before executing PR 3, re-verify the 7/20 findings:**
- `src/lib/api-keys.ts` is **USED** by `src/app/api/keys/create/route.ts` (`import { generateApiKey } from "@/lib/..."`). The 7/20 scan was wrong — **KEEP this file**.
- `src/lib/color-templates.ts` is **USED** by `src/app/studio/tools/ColorByNumberTool.tsx`, `src/components/color/ColorByNumber.tsx`, and `src/components/color/TemplateGallery.tsx` (4 imports total). The 7/20 scan was wrong — **KEEP this file**.

**The real dead `src/lib/` list (re-verified today):**
- Safe to delete: `src/lib/AgentOrchestrator.ts` (duplicate of class in `agents.ts`), `src/lib/api.ts` (zero importers), `src/lib/layout-schema.ts` (zero importers), `src/lib/projects.ts` (superseded by `studio-projects.ts`), `src/lib/skybox.ts` (zero importers), `src/lib/agent-profiles.ts` (zero importers).
- DO NOT delete: `src/lib/api-keys.ts` (used by `/api/keys/create`), `src/lib/color-templates.ts` (used by 3 color components).

### M-2. `tsc_errors.txt` still at root and not gitignored

`Test-Path tsc_errors.txt` → **EXISTS**. The `.gitignore` lists `tsc-out.txt` but not `tsc_errors.txt`. The file contains a `TS6054` / `TS6231` error from a malformed `tsc tsc-out.txt type` command.

**Action:** `Remove-Item tsc_errors.txt` and add `tsc_errors.txt` to `.gitignore`.

### M-3. 6 unused npm deps still in `package.json`

The 7/20 scan confirmed 0 imports for: `@usejarvis/brain`, `jsnes`, `@monaco-editor/react`, `monaco-editor`, `@stripe/stripe-js`, `playwright` (devDep).

**Caveat for monaco:** `src/app/studio/components/StudioMonacoEditor.tsx` exists. The 7/21 scan §M-5 specifically flagged this — verify it's truly unused, or if it does load Monaco via dynamic import, the dep is actually used.

**Action:** `pnpm remove @usejarvis/brain jsnes @stripe/stripe-js`; re-verify monaco; `pnpm remove -D playwright`.

### M-4. `terminal-server/fly.toml` is orphan

`Test-Path terminal-server/fly.toml` → **EXISTS**. No CI workflow references it. `terminal-server/railway.json` is the canonical target.

**Action:** Delete.

### M-5. Root `Dockerfile` is broken

Per 7/20 §5 and 7/21 §M-8: uses `output: "standalone"` pattern but `next.config.ts` does not set that flag. Won't produce a valid image.

**Action:** Either add `output: "standalone"` (with a comment "self-host only — not used by Vercel"), or delete the file. `docker/Dockerfile.terminal` is the only canonical image.

### M-6. `src/proxy.ts` purpose unclear

`Test-Path src/proxy.ts` → **EXISTS**. Per 7/20 §7, this was flagged as "potentially obsolete." No `pnpm` script invokes it.

**Action:** Grep for `src/proxy` across `package.json`, `.github/workflows/*.yml`, and `scripts/` to find callers. If none, archive.

### M-7. `src/daemon.ts` is wired nowhere

`Test-Path src/daemon.ts` → **EXISTS**. Per `tasks/active.json`, the daemon is the worker process that claims tasks from `/api/agent-tasks`. No `pnpm` script starts it.

**Action:** Add `"daemon:dev": "tsx src/daemon.ts"` and document the deploy story (Railway sidecar? Render background worker?).

### M-8. `docs/` has 5 stale reports and no index

| File                                                  | Age (days) | Status                          |
| ----------------------------------------------------- | ---------- | ------------------------------- |
| `docs/page-audit-report.md`                           | 47         | Stale; contains a malformed path |
| `docs/LITLABS_WORKSPACE_DEEP_DIVE_2026-07-14.md`       | 11         | Written for the archived checkout |
| `docs/deep-scan-report.md` (2026-07-02)               | 23         | Stale; contradicts current TS strict and missing-tables claims |
| `docs/deep-scan-2026-07-21.md`                        | 4          | **Also stale now** — its "Resolved" claims are wrong (see §1) |
| `docs/scan-needs.md`                                  | —          | Fragmented audio-fix note; integrate or delete |

`docs/` is missing a `docs/README.md` index. A new contributor will not know which report is current.

**Action:**
1. Add `docs/README.md` pointing at `deep-dive-2026-07-14.md` and this report as the two live sources.
2. Move the 3 truly stale ones to `docs/legacy/` (the directory already exists).
3. Delete `page-audit-report.md` and `LITLABS_WORKSPACE_DEEP_DIVE` outright.
4. Stamp `deep-scan-report.md` and `deep-scan-2026-07-21.md` as "SUPERSEDED 2026-07-25".

### M-9. `0%` test coverage of `src/lib/`

`tests/` contains only:
- `tests/code-scanner.test.ts`
- `tests/terminal-auth.test.ts`
- `tests/terminal-security.test.ts`
- `tests/scripts/`

**Zero coverage** of: `llm.ts` (failover chain), `agents.ts` (orchestrator), `litt.ts` (notifications), `rate-limiter.ts` (serverless-safe), `r2.ts` (signed URLs + LRU), `user-db.ts` (Clerk→UUID), `wallet-ledger.ts`, Stripe webhook handler, Clerk webhook handler.

**Risk:** Regressions in the LLM failover chain, wallet credit math, or webhook signature verification will ship undetected.

**Action:** Set a 7-day target: unit tests for `llm.ts`, `rate-limiter.ts`, `user-db.ts`, `wallet-ledger.ts`, `/api/stripe/webhook`, `/api/webhook/clerk`. The Settings test pattern (smoke test that a value round-trips) is already proven — replicate it.

---

## 4. LOW-severity issues (current)

### L-1. `marketplace/page.tsx` has 1 empty Stripe priceId
Per 7/14 deep-dive §4.4, 4 of 5 credit packs have IDs; 1 is still `""`. Create the missing Stripe price and paste the ID.

### L-2. `docker-compose.yml` uses deprecated `version: '3.8'`
Harmless on Compose v2 but cosmetically stale. One-line cleanup.

### L-3. `terminal-server/jarvis-ai.ts` is the last surviving "Jarvis" name
Per `.clinerules` this is intentional. Cosmetic-only.

### L-4. `src/app/og-image.png/route.tsx` is awkwardly named
Per 7/14 deep-dive §3.3. Rename to `src/app/og/route.tsx`.

### L-5. `chat` API route (H-6 from 7/2) — verify state
7/2 said `/api/chat` was in-memory. The wiring map shows it's used by `gallery/[id]` (legacy). Either delete the route or move its data to Supabase.

### L-6. `users/plan` in-memory Map (H-7 from 7/2)
Per 7/2 this was an in-memory `Map()`. Verify the current implementation is DB-backed.

---

## 5. Quick-Win Action Plan

### Today (≤30 min, zero risk)
1. **Delete `tsc_errors.txt`** at root; add `tsc_errors.txt` to `.gitignore`.
2. **Spot-check** `MobileMoreSheet` — is it still imported? If not, remove the orphan file.
3. **Spot-check** `docs/scan-needs.md` — integrate the audio fix into a real doc or delete it.

### This week (1–2 hours)
4. **Reconcile `.clinerules`** (H-1): update `WORKSPACE_ROOT` to the current `litlab` path; remove `AgentOrchestrator.ts` from the subsystem list; add `agents.ts` instead.
5. **Delete `src/lib/AgentOrchestrator.ts`** (the duplicate class) AND `src/lib/agent-profiles.ts`, `src/lib/api.ts`, `src/lib/layout-schema.ts`, `src/lib/projects.ts`, `src/lib/skybox.ts` (verified 0 importers today). Run `npx tsc --noEmit` after.
6. **Fix or delete `AGENTS.md`** (H-1).
7. **Decide on the uncommitted Studio redesign** (H-2): commit it or revert it. Run `pnpm build` either way.

### Next week (4–8 hours)
8. **Add tests for `src/lib/` core** (M-9): unit tests for `llm.ts`, `rate-limiter.ts`, `user-db.ts`, `wallet-ledger.ts`.
9. **Drop the confirmed-unused deps** (M-3): `pnpm remove @usejarvis/brain jsnes @stripe/stripe-js playwright`; re-verify monaco first.
10. **Delete orphaned public assets** (M-1 PR 2): `public/{file,globe,next,vercel,window}.svg`, `public/logo~2.png`, `public/og-image.png`.
11. **Delete `terminal-server/fly.toml`** (M-4).
12. **Autonomic Loop e2e smoke** (H-3): add `"daemon:dev": "tsx src/daemon.ts"`; sign in → POST `/api/director/plan` → POST `/api/agent-tasks` → poll until `completed` or 30s timeout.

### This month
13. **Clean `docs/`** (M-8): add `docs/README.md` index, move 3 stale reports to `docs/legacy/`, stamp `deep-scan-report.md` and `deep-scan-2026-07-21.md` as superseded.
14. **Migrate `marketplace/page.tsx` last Stripe price ID** (L-1).
15. **Document or delete the broken root `Dockerfile`** (M-5).
16. **Audit `MobileMoreSheet`, `MOBILE_MORE_ITEMS`, and `src/proxy.ts`** for true dead-code status.

---

## 6. Things Explicitly NOT Needed (verified)

- Do NOT delete `src/lib/api-keys.ts` or `src/lib/color-templates.ts` — both are LIVE (imports verified today).
- Do NOT remove `llm-executor.ts`, `llm-completion.ts`, `agent-worker.ts` — used by `src/daemon.ts`.
- Do NOT remove `keytar` from `package.json` — transitive dep used by `agentCommands.ts` via `require()`.
- Do NOT scaffold a new `package.json` or `tsconfig.json` — the strict TS config is correct.
- Do NOT rename `terminal-server/jarvis-ai.ts` to `litt-ai.ts` — `.clinerules` explicitly keeps it.
- Do NOT consolidate `@google/generative-ai` and `@google/genai` — they expose different APIs.
- Do NOT trust the 7/21 deep-scan's "Resolved" claims — re-verify every one before acting.

---

## 7. Verification Commands (re-run any time)

```powershell
# Health
npx tsc --noEmit
pnpm build
pnpm lint
pnpm test

# File-system hygiene
Test-Path tsc_errors.txt, query, FACEBOOK_FOLLOWER_GROWTH_CHECKLIST.md, render.yaml, public/og.png
Test-Path src\lib\api-keys.ts, src\lib\api.ts, src\lib\color-templates.ts, src\lib\layout-schema.ts, src\lib\projects.ts, src\lib\skybox.ts, src\lib\agent-profiles.ts, src\lib\AgentOrchestrator.ts
Test-Path public\file.svg, public\globe.svg, public\next.svg, public\vercel.svg, public\window.svg, public\logo~2.png, public\og-image.png
Test-Path terminal-server\fly.toml

# Stale references in rules
Select-String -Path .clinerules -Pattern 'AgentOrchestrator.ts|litt.ts.*brain'
Select-String -Path .cursorrules -Pattern 'AgentOrchestrator.ts'
Select-String -Path AGENTS.md -Pattern 'litt.ts.*brain'

# Autonomic Loop banner health
curl http://localhost:3000/api/director/plan
curl http://localhost:3000/api/agents
curl http://localhost:3000/api/memory
curl http://localhost:3000/api/agent-tasks
```

---

## 8. One-Sentence Verdict

The codebase is **healthier than the 7/21 scan claimed but messier than the 7/21 scan admitted** — the cleanup the prior reports recommended was never executed, the rule files now contradict each other, and 6 files of Studio mobile redesign are sitting uncommitted on the working tree. Fix H-1 (rule-file drift), H-2 (commit or revert the redesign), and H-3 (run the autonomic loop e2e) in that order, and re-execute the 7/20 4-PR cleanup plan with the corrected dead-file list, and the project returns to its true best state.

