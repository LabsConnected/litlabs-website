# LiTTree Lab Studios — Deep Scan & "What's Needed" Report

**Date:** 2026-07-26
**Auditor:** Cline (act mode)
**Working dir:** `c:\Users\litbi\CascadeProjects\litlab`
**HEAD:** `d43867105cd22db9543b1da5271b86457483fe8e`
**Previous scans:** `deep-scan-2026-07-25.md` (1 day old), `deep-scan-2026-07-21.md` (5 days old, now in `docs/legacy/`), `deep-dive-2026-07-14.md` (12 days old)

> ✅ **The 7/25 deep-scan's complaints have all been resolved by commits between 7/25 and 7/26.** The recommended cleanup from 7/20/7/21/7/25 was executed in `49cfaf44`, `2944f8a9`, and `c863d9da`. The rule files are now aligned. The dead files, orphan assets, `tsc_errors.txt`, and `docs/` rot are gone.
>
> 🟡 **What is needed today:** ship the 43-file SEO + home-page + game-player push that is sitting uncommitted on the working tree, and fix one new docker-compose regression.

---

## 0. Executive Verdict

**Health:** 🟡 **Drifted in a different direction.** The code is shipping, and the old cleanup is done — but a *new* large uncommitted diff is on the working tree, and the docker-compose self-host path regressed when the root `Dockerfile` was deleted.

| Metric                              | Value                                                |
| ----------------------------------- | ---------------------------------------------------- |
| Open critical issues                | **2** (uncommitted SEO push + broken `docker-compose.yml`) |
| Open high issues                    | **2** (autonomic-loop POST e2e + Stripe priceId)     |
| Open medium issues                  | **4** (file hygiene + tests + rule drift)            |
| Open low issues                     | **3**                                                 |
| **Total open**                      | **~11**                                              |
| Files in `src/lib/`                 | 57 (4 directories + 53 files)                        |
| Dead `src/lib/` files (verified)    | **0** (all 6 prior flagged files now deleted)        |
| Orphaned public assets (verified)   | **0** (all 7 prior flagged assets now deleted)       |
| Uncommitted changes                 | **43 modified + 25 untracked**, +1082/−820 lines     |
| `tsc_errors.txt` at root            | **GONE** (gitignored)                                |
| Root `Dockerfile`                   | **GONE** (M-5 resolved, but docker-compose is now broken — see C-2) |
| `src/proxy.ts`                      | **GONE** (M-6 resolved)                              |
| `AGENTS.md`                         | **GONE**                                             |
| `daemon:dev` script                 | **EXISTS** (`tsx src/daemon.ts`)                     |

---

## 1. What changed since 7/25 — net positive

Between 7/25 (HEAD `7587afd0`) and today (HEAD `d4386710`), **22 commits** landed. The relevant ones for this scan:

| Commit     | Title                                                                              | What it fixed                          |
| ---------- | ---------------------------------------------------------------------------------- | -------------------------------------- |
| `49cfaf44` | cleanup: delete 6 dead src/lib files, orphaned MobileMoreSheet, fly.toml, 7 public assets; fix .clinerules WORKSPACE_ROOT; gitignore tsc_errors.txt | M-1, M-2, M-4, M-6, M-7, H-1 |
| `c863d9da` | chore: clean docs/ — move 6 stale reports to legacy/, add README.md index          | M-8                                    |
| `2944f8a9` | fix: deep-scan cleanup across security, deps, DB, and dead code                   | M-1 (broader cleanup)                  |
| `177e14b3` | chore: sync lockfile after dep removal, fix unused catch params                   | M-3                                    |
| `13d8482f` | fix(clerk): always call clerkMiddleware() to fix 500s when env vars missing        | Clerk middleware fix                   |
| `2839a9f5` | fix(autonomic-loop): mount banner on all routes, add GET handler to director/plan | H-3 (partial — banner mounted, GET works) |
| `c8518e3f` | chore: remove unused imports and state bindings                                   | Lint cleanup                           |
| `d4386710` | chore: add Devin CLI config, skills, and litlab-explorer subagent                 | Devin integration                      |

**Net result:** every HIGH and MEDIUM item from the 7/25 scan is RESOLVED. The codebase is now healthier than at any point in the last 12 days.

---

## 2. CRITICAL — New issues found today (7/26)

### C-1. Massive uncommitted SEO + home-page + game-player push (43 modified + 25 untracked files)

`git status --short` shows **43 modified files (+1082/−820 lines)** and **25 untracked files**. This is roughly **6× the size** of the 7/25 H-2 uncommitted redesign (6 files, +220/−183).

**Coherent themes in the uncommitted work:**

| Theme | Files | Risk |
|-------|-------|------|
| **SEO infrastructure** | `src/lib/seo.ts` (new), `src/components/seo/JsonLd.tsx` (new), `scripts/generate-seo-images.mjs` (new), `public/og/littree-labstudios.jpg` (new), `public/icon-512.png` (new), `src/app/icon.png` (new), `src/app/apple-icon.png` (new), `src/app/robots.ts` (rewrite, 56 lines), `src/app/sitemap.ts` (rewrite, 104 lines) | 🟢 Low — all logic checked, looks correct, OG image exists |
| **Per-route layouts** | 18+ new `layout.tsx` files at `src/app/{admin,agents/[slug],code,dashboard,deployments,flow,gallery/[id],games/cloud,games/dos,games/retro,games/retro/play/[gameId],library,marketplace,memories,order,pricing,profile/[username],projects,resources,settings,showcase,sign-in,sign-up,social,voice,wallet}` | 🟡 Medium — pattern is clean (use `buildMetadata()` for indexed routes, `robots: { index: false }` for private) but many functions are all named `NoIndexLayout` (cosmetic, no Next.js error) |
| **Home page refactor** | `src/app/page.tsx` (−556 lines), `src/app/HomePageClient.tsx` (new, 494 lines) | 🟢 Low — the extracted client is a clean implementation; the server page now just exports `metadata` |
| **Game player rewrite** | `src/app/games/retro/play/[gameId]/page.tsx` (+784 lines, now 929 total) | 🟡 Medium — large diff; uses self-hosted EmulatorJS at `/emulatorjs/4.2.3/data/` (CDN death was the trigger); needs a manual smoke test on `/games/retro/play/<gameId>` |
| **Self-hosted EmulatorJS** | `public/emulatorjs/4.2.3/` (new directory) | 🟢 Low — supports the game player rewrite |
| **Public asset cleanup** | `public/robots.txt` (deleted) replaced by `src/app/robots.ts`; `src/app/icon.tsx` (deleted, 32 lines) replaced by `public/icon-512.png` | 🟢 Low — Next.js supports both `app/icon.{png,tsx}` and `public/icon-512.png` (the former is auto-discovered) |
| **Site-wide metadata** | `src/app/layout.tsx` (+98 lines) | 🟢 Low — adds `metadataBase`, title template, Google verification, theme color, etc. |
| **Touch-ups across all layouts** | All ~30 `src/app/*/layout.tsx` files + `src/app/cookies/page.tsx`, `src/app/privacy/page.tsx`, `src/app/terms/page.tsx`, `src/app/sign-in/[[...sign-in]]/page.tsx`, `src/app/docs/{page,DocsPageClient}.tsx`, `src/app/{dashboard,settings,profile,marketplace,gallery,games,agent,agents,ai-builder,code,flow,social,studio,deployments}/layout.tsx`, `src/app/global-error.tsx`, `src/app/not-found.tsx`, `src/components/litt-terminal/LiTTTerminalPage.tsx` | 🟡 Medium — small, mechanical metadata/robots/className tweaks. Need a single `pnpm build` to confirm none break the build |

**Why this is critical right now:**

1. A `git pull`, branch switch, or merge conflict resolution would lose 43 files of work.
2. `pnpm build` and `npx tsc --noEmit` have **not** been run on this diff (per `.todo.md`, the last typecheck was on the previous commit).
3. The game player rewrite in particular is 929 lines with a self-hosted EmulatorJS path — easy to break.
4. The home page now has a `useEffect` that auto-redirects signed-in users to `/studio` (correct), but it depends on `useClerkAuth()` and `useSupabaseAuthHook()` — both hooks need to be exercised.

**Recommended commit plan (5 PRs, in order):**

1. **PR A — SEO infra**: `src/lib/seo.ts`, `src/components/seo/JsonLd.tsx`, `src/lib/siteConfig.ts`, `scripts/generate-seo-images.mjs`, `public/og/littree-labstudios.jpg`, `public/icon-512.png`, `src/app/icon.png`, `src/app/apple-icon.png`, `src/app/robots.ts`, `src/app/sitemap.ts`, `public/robots.txt` (delete), `src/app/icon.tsx` (delete).
2. **PR B — Per-route layouts (private/noindex first)**: `admin`, `agent`, `agents`, `agents/[slug]`, `dashboard`, `library`, `memories`, `profile`, `profile/[username]`, `projects`, `settings`, `wallet`, `voice`, `order`, `code`, `flow`, `ai-builder`, `social`, `deployments`. Each is independent.
3. **PR C — Per-route layouts (public/indexed)**: `gallery`, `gallery/[id]`, `games`, `games/cloud`, `games/dos`, `games/retro`, `games/retro/play/[gameId]` (layout only), `marketplace`, `pricing`, `resources`, `showcase`, `sign-in`, `sign-up`.
4. **PR D — Home page refactor**: `src/app/page.tsx`, `src/app/HomePageClient.tsx`, `src/app/layout.tsx` (site-wide metadata), `src/app/global-error.tsx`, `src/app/not-found.tsx`. The remaining touch-up files (cookies/privacy/terms/sign-in/docs etc.) can go here too.
5. **PR E — Game player rewrite + EmulatorJS self-host**: `src/app/games/retro/play/[gameId]/page.tsx`, `public/emulatorjs/4.2.3/`. This is the riskiest PR; it should ship last so it can be reverted cleanly.

**Action items:**
- [ ] Decide which PR(s) to ship first. **The SEO infra (PR A) and the noindex layouts (PR B) are zero-risk** and can ship today.
- [ ] Run `npx tsc --noEmit` between each PR.
- [ ] Run `pnpm build` between each PR.
- [ ] Smoke test `/`, `/pricing`, `/admin`, `/games/retro/play/<gameId>` after PR E lands.

### C-2. `docker-compose.yml` is broken — the `app` service references a deleted `Dockerfile`

`docker-compose.yml` line 7 still says:

```yaml
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
```

But `Test-Path Dockerfile` returns **False**. The root `Dockerfile` was deleted (M-5 was solved by deletion). The compose file was not updated.

**Consequence:** anyone running `docker-compose up` will get a build error: `failed to solve: failed to read dockerfile: open Dockerfile: no such file or directory`.

**Action:** Pick one:
- (A) Add `output: "standalone"` to `next.config.ts` and recreate a working root `Dockerfile`. Document it as "self-host only — not used by Vercel."
- (B) Remove the `app` service from `docker-compose.yml` (Vercel is the canonical deploy target; the compose file becomes terminal-only).
- (C) Repoint the `app` build to `docker/Dockerfile.terminal` — but **this is wrong**: that image is the terminal server, not the Next.js app. **Do not do this.**

**Recommendation:** (B) is the cleanest. The repo is deployed on Vercel; the compose file is for local terminal testing. The terminal service still builds fine.

---

## 3. HIGH-severity issues (carryover from 7/25)

### H-1. Autonomic Loop e2e POST path still unverified

`tasks/active.json` still says:
> "Next: verify with pnpm dev that all 4 endpoints return 2xx for a logged-in user, confirm the banner shows 'ok' on the dashboard, then roll forward to the next milestone."

The **GET** path is verified (banner mounted, `/api/director/plan` GET handler added in `2839a9f5`). The **POST** path — sign in → POST `/api/director/plan` → POST `/api/agent-tasks` → poll until `completed` or 30s timeout — has never been exercised end-to-end.

**Action:**
1. `pnpm dev:all` to bring up Next + terminal + daemon.
2. Sign in via Clerk.
3. `curl -X POST http://localhost:3000/api/director/plan -H 'Cookie: ...'` with a sample task body.
4. `curl -X POST http://localhost:3000/api/agent-tasks -H 'Cookie: ...'`.
5. Poll `/api/agent-tasks` until the claim resolves.
6. Fix any 4xx/5xx and update `tasks/active.json` when the smoke passes.

### H-2. `marketplace/page.tsx` empty Stripe priceId (L-1 carryover)

Per 7/14 deep-dive §4.4, 4 of 5 credit packs have IDs; 1 is still `""`. **Status today:** not yet verified. The marketplace file was modified in the uncommitted push — confirm whether the empty ID was filled in or is still blank.

**Action:** Open `src/app/marketplace/page.tsx`, find the empty `priceId`, and either fill it from Stripe or remove that pack.

---

## 4. MEDIUM-severity issues (current)

### M-1. `$null` file at root is being modified

`git status --short` shows `M $null`. This is a PowerShell shell artifact (created by `> $null` redirection mistakes) and contains no useful content. It should be deleted and added to `.gitignore` (with a leading backslash so `$` is literal: `\$null`).

**Action:** `Remove-Item -LiteralPath '$null' -ErrorAction SilentlyContinue` and add `\$null` to `.gitignore` (line ~57, after `tsc_errors.txt`).

### M-2. `next-env.d.ts` modified in working tree

`next-env.d.ts` is a Next.js auto-generated file (line 55 of `.gitignore` proves this). It was modified in the uncommitted diff. Per .gitignore, **it should not be in git at all** — it appears in `git status` only because it was committed in the past.

**Action:** `git rm --cached next-env.d.ts` and commit, so future regenerations stop showing up in `git status`.

### M-3. `vercel-inspect.json` untracked

A new `vercel-inspect.json` is untracked. It appears to be a one-off Vercel inspection artifact (not a build config — Vercel uses `vercel.json` for that). 

**Action:** Either delete it (if it was a one-off inspection) or add to `.gitignore` if it will be regenerated.

### M-4. `src/lib/` is now 0%-tested (M-9 carryover)

The 7/25 report flagged this; nothing has changed. `tests/` still covers only `code-scanner`, `terminal-auth`, `terminal-security`, and `workspace-security`.

**Action:** Same 7-day target: unit tests for `llm.ts`, `rate-limiter.ts`, `user-db.ts`, `wallet-ledger.ts`, `/api/stripe/webhook`, `/api/webhook/clerk`. The Settings round-trip pattern is proven.

---

## 5. LOW-severity issues (current)

### L-1. `NoIndexLayout` function name reused across 18+ files

The new private-route `layout.tsx` files all export a function named `NoIndexLayout`. TypeScript and Next.js are fine with this (it's a local export, not a global), but it's confusing when grepping.

**Action:** Rename to `<RouteName>Layout` (e.g. `AdminLayout`, `WalletLayout`) for readability. No urgency.

### L-2. `vercel.json` content vs `vercel-inspect.json`

`vercel.json` exists and is canonical. `vercel-inspect.json` is untracked. Verify they don't conflict; if `vercel-inspect.json` is dead, delete it.

### L-3. `src/app/icon.tsx` deleted in favor of `public/icon-512.png`

Next.js will auto-discover `public/icon-512.png` for `<link rel="icon">`, and `src/app/apple-icon.png` for the Apple touch icon. Both are generated by `scripts/generate-seo-images.mjs`. No action needed unless Lighthouse complains about icon dimensions.

---

## 6. Quick-Win Action Plan

### Today (≤15 min, zero risk)
1. **Delete `$null`** at root; add `\$null` to `.gitignore`.
2. **Delete `vercel-inspect.json`** if it's a one-off; otherwise add to `.gitignore`.
3. **`git rm --cached next-env.d.ts`** so future regenerations stop polluting `git status`.
4. **Decide on `docker-compose.yml`**: option (B) — remove the `app` service. ~5 lines of YAML.

### This week (1–3 hours)
5. **Ship PR A (SEO infra)**: `src/lib/seo.ts`, `src/components/seo/JsonLd.tsx`, `src/lib/siteConfig.ts`, `scripts/generate-seo-images.mjs`, `public/og/`, `public/icon-512.png`, `src/app/{icon,apple-icon}.png`, `src/app/{robots,sitemap}.ts`, delete `public/robots.txt` and `src/app/icon.tsx`. Run `npx tsc --noEmit` + `pnpm build`.
6. **Ship PR B (noindex layouts)**: 18 private-route `layout.tsx` files. Run typecheck + build.
7. **Ship PR C (indexed layouts)**: the rest of the route layouts. Run typecheck + build.
8. **Run autonomic loop e2e POST smoke** (H-1).

### Next week (4–8 hours)
9. **Ship PR D (home page refactor)**: `src/app/page.tsx`, `src/app/HomePageClient.tsx`, `src/app/layout.tsx`, plus the touch-up files (cookies/privacy/terms/sign-in/docs).
10. **Ship PR E (game player)**: `src/app/games/retro/play/[gameId]/page.tsx` + `public/emulatorjs/4.2.3/`. Manual smoke test on `/games/retro/play/<gameId>`.
11. **Add the missing Stripe priceId** in `src/app/marketplace/page.tsx` (H-2).
12. **Tests for `src/lib/`** (M-4): unit tests for `llm.ts`, `rate-limiter.ts`, `user-db.ts`, `wallet-ledger.ts`.

### This month
13. Rename `NoIndexLayout` → `<RouteName>Layout` for grep readability (L-1).
14. Update `docs/README.md` to add a link to this report (today's `docs/deep-scan-2026-07-26.md`).
15. Mark `docs/deep-scan-2026-07-25.md` as "SUPERSEDED 2026-07-26" and move to `docs/legacy/`.

---

## 7. Things Explicitly NOT Needed (verified)

- Do NOT add a root `Dockerfile` unless you adopt `output: "standalone"` in `next.config.ts` AND accept the build complexity. The C-2 fix should be (B) — remove the `app` service from `docker-compose.yml`.
- Do NOT remove `@monaco-editor/react` or `monaco-editor` from `package.json`. They are used by `src/components/litt-terminal/CodeEditor.tsx` (verified via `rg`).
- Do NOT remove `keytar` from `package.json` — transitive dep used by `agentCommands.ts`.
- Do NOT consolidate `@google/generative-ai` and `@google/genai` — they expose different APIs.
- Do NOT bring back `AGENTS.md` or `src/lib/AgentOrchestrator.ts` — both are intentionally gone.
- Do NOT touch `.clinerules` or `.cursorrules` — they are aligned as of `49cfaf44`.
- Do NOT move `public/emulatorjs/` to a CDN — the 7/25 commit `97c7767c` was specifically about fixing CDN death / cache corruption. Self-host is the correct posture.
- Do NOT scaffold a new `package.json` or `tsconfig.json` — strict TS config is correct.
- Do NOT trust the 7/25 deep-scan's "Resolved" claims — they are now actually true, but verify any *new* claim with `Test-Path` or `rg` before acting.

---

## 8. Verification Commands (re-run any time)

```powershell
# Health
npx tsc --noEmit
pnpm build
pnpm lint
pnpm test

# File-system hygiene
Test-Path tsc_errors.txt, '$null', Dockerfile, render.yaml, public\robots.txt, public\og.png
Test-Path src\lib\AgentOrchestrator.ts, src\lib\api.ts, src\lib\layout-schema.ts, src\lib\projects.ts, src\lib\skybox.ts, src\lib\agent-profiles.ts, src\proxy.ts, src\app\icon.tsx, src\app\StudioMonacoEditor.tsx
Test-Path public\file.svg, public\globe.svg, public\next.svg, public\vercel.svg, public\window.svg, public\logo~2.png, public\og-image.png
Test-Path terminal-server\fly.toml

# Uncommitted work
git status --short
git diff --stat HEAD

# Rule drift
Select-String -Path .clinerules -Pattern 'AgentOrchestrator|litt.ts.*brain'
Select-String -Path .cursorrules -Pattern 'AgentOrchestrator'

# Autonomic Loop banner health
curl http://localhost:3000/api/director/plan
curl http://localhost:3000/api/agents
curl http://localhost:3000/api/memory
curl http://localhost:3000/api/agent-tasks

# docker-compose sanity
docker compose config --quiet   # exits non-zero if YAML invalid
```

---

## 9. One-Sentence Verdict

The 7/20+7/21+7/25 cleanup recommendations were actually executed in commits `49cfaf44` and `c863d9da` — every old complaint is gone — but a new, much larger 43-file SEO + home-page + game-player push is sitting uncommitted on the working tree, and the root-`Dockerfile` deletion regressed `docker-compose.yml`. Ship PR A + PR B today, fix `docker-compose.yml` (option B), and run the autonomic-loop POST smoke, and the project is back to its best state in weeks.
