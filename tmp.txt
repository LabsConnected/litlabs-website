items.length === 0 ? (
            <div className="rounded-xl p-3 text-center text-[11px] font-medium text-white/50">
              No new notifications
            </div>
          ) : (
            items.map((it, i) => {
            const Icon = it.icon;
            return (
              <div
                key={i}
                className="flex items-start gap-2 rounded-xl p-2 transition-col
