/**
 * Compatibility export for the builder's historical import path.
 * The implementation now lives in the single Asset Lake surface used by
 * Studio context, canvas, and the mobile context drawer.
 */
export { default as AssetsPanel } from "../../context/AssetsPanel";
