"use client";

// LoopsTool — registered alias for the PipelineTool.
// Exposes the existing media-flow pipeline under the "loops" tool id in the
// Studio sidebar so users can access it without it being hidden as an
// unregistered file.

export { default } from "./PipelineTool";
