fix: align all responsive breakpoints to md (768px)

The nav components had inconsistent breakpoints:
- Hamburger, desktop nav, bottom nav, drawer: switched at md (768px)
- Logo text: switched at lg (1024px)

This created a "no man's land" between 768-1024px where the layout
was a confusing hybrid: icon-only logo (mobile style) + desktop nav
links (desktop style) + no hamburger + no bottom nav + no drawer.
Users couldn't tell if they were on mobile or desktop.

Fix: aligned ALL breakpoints to md (768px) across 3 components:
- NavbarWrapper.tsx: logo switches at md (was lg), nav link sizes
  adjusted for md, Marketplace link shows at lg (was xl)
- CommandStudioHeader.tsx: logo switches at md (was lg)
- LandingHeader.tsx: logo switches at md (was sm)

Now the behavior is clean and consistent:
- < 768px (mobile): hamburger + icon-only logo + bottom nav + drawer
- >= 768px (desktop): logo with text + desktop nav links + search/bell

Quality gate: typecheck PASS, lint 0 errors/48 warnings, 192 pages

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
