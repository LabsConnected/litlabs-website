feat: premium UX polish pass — header, sidebar, dashboard, bottom nav

Major UX polish to make the app feel premium instead of a website.

1. Header (NavbarWrapper):
   - Logo 30px → 38px (25% bigger)
   - Hide "LiTTree LabStudios" text below lg (was md)
   - Header height 14→16, touch targets 9→10 grid
   - Mobile: only hamburger + logo + settings (removed clutter)
   - Search/bell only on md+ with proper grid sizing

2. Sidebar (Sidebar.tsx):
   - Nav item height 10→12, gap 2.5→3, padding 3→3.5
   - space-y-1 → space-y-2 (more breathing room)
   - Icon 16→18, font 11→12px
   - Section labels: mb-1.5→2.5, mt-4→6

3. Dashboard cards (MissionControlDashboard MetricCard):
   - Added colored status dots with glow (green/amber/red/dim)
   - hover:-translate-y-0.5 → hover:-translate-y-1 + scale-[1.02]
   - Added backdrop-filter blur(12px) for glass effect
   - Each metric gets contextual status color:
     Active Missions: green if >0, dim if empty
     Needs Attention: red if >0, green if 0
     LiTTBits: purple if >0, dim if 0
     Workspace: green=ready, amber=other, dim=none
     Terminal: green=connected, amber=sleeping, dim=none
     Deployment: green=production, amber=other, dim=none

4. Mission hero:
   - "Run the platform. Finish the mission." → "Welcome back, {name}."
   - Subtitle → "LiTT has your workspace online and is ready
     for your next mission."
   - Uses Clerk useUser() for personalized name

5. Background (AnimatedBackground):
   - Sparkles 15 → 40 (more stars)
   - Larger size range (0.5-2.5 vs 0.5-2)
   - Slower fade for more visible twinkling
   - Added scroll parallax — stars drift at 0.15x scroll speed

6. Bottom nav (MobileBottomNav) — full redesign:
   - 5-slot layout: Home | Studio | [+ Create] | Discover | Me
   - Center Create button: 48px rounded-2xl, gradient fill,
     glow shadow, scale-on-press
   - Home replaces Dashboard, Me replaces Gallery
   - Active indicator bar with glow

7. Live activity feed:
   - Status dots now have glow shadow (0 0 8px {color}60)
   - Dots scale 125% on hover
   - Row hover: subtle bg + padding + rounded
   - Dot size 2→2.5

8. Terminal state:
   - "disconnected" → "{label} sleeping" (friendlier)
   - Sleeping badges get pulsing dot animation
   - Color: amber (not red) for sleeping states

Quality gate: typecheck PASS, lint 0 errors/48 warnings, 192 pages

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
